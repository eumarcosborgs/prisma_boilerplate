## :checkered_flag: Começando ##

```bash
# Clone this project
$ git clone git@github.com:eumarcosborgs/prisma_boilerplate.git

# Access
$ cd prisma_boilerplate

# Install dependencies
$ yarn

# Add environment variables in .env

# Run the project
$ yarn dev

# The server will initialize in the <http://localhost:3000>
```